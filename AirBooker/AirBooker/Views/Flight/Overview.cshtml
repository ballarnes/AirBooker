@using Microsoft.AspNetCore.Identity;
@model AirBooker.Web.Models.Flight.OverviewViewModel
@inject SignInManager<ApplicationUser> SignInManager
@inject UserManager<ApplicationUser> UserManager

@{
    ViewData["Title"] = Model.Flight.ArrivalAirport.Place;
}

<div class="card mb-3">
    <div class="row g-0">
        <div class="card-body">
            <div class="row">
                <div class="col">
                    <h4 class="display-6">Ticket:</h4>
                    <partial name="_TicketInfoCardPartial" model="@Model.Flight" />
                </div>
                @if (Model.WithReturnFlight)
                {
                    <div class="col">
                        <h4 class="display-6">Return ticket:</h4>
                        <partial name="_TicketInfoCardPartial" model="@Model.ReturnFlight" />
                    </div>
                }
            </div>
            <div class="row mt-3">
                <div class="col">
                    <h4 class="display-6">Personal info:</h4>
                    <div class="card">
                        <ul class="list-group list-group-flush">
                            <li class="list-group-item">
                                <div class="input-group">
                                    <span class="input-group-text">Email:</span>
                                    <input type="text" class="form-control" value="@User.Identity!.Name" disabled readonly>
                                </div>
                            </li>
                        </ul>
                    </div>
                </div>
            </div>
            <div class="row text-center mt-3">
                <h6>Found a mistake? Turn back and change the data right now. In the next steps it will be impossible.</h6>
            </div>
            <div class="mt-3">
                <div class="d-flex justify-content-center">
                    <a asp-controller="Payment" asp-action="Index" asp-route-flightId="@Model.Flight.Id" asp-route-returnFlightId="@Model.ReturnFlight?.Id">
                        <input type="button" class="btn btn-outline-success" value="Proceed to payment" />
                    </a>
                </div>
            </div>
        </div>
    </div>
</div>